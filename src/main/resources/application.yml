spring:
  application:
    name: petstoreorderservice
server:
  port: ${PETSTOREORDERSERVICE_SERVER_PORT:8083}

petstore:
  service:
    product:
      url: ${PETSTOREPRODUCTSERVICE_URL:http://localhost:8082}
    order-items-reserver:
      url: ${ORDER_ITEMS_RESERVER_URL:http://localhost:62301}
      x-functions-key: ${ORDER_ITEMS_RESERVER_FUNCTION_KEY}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha
  show-actuator: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
      show-components: always
  health:
    ssl:
      enabled: false
    diskspace:
      enabled: false

logging:
  level:
    root: INFO
    com.chtrembl: DEBUG
